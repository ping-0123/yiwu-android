<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yinzhiwu.yiwu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.yinzhiwu.sys.YiwuApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".OkHttpActivity" />
        <activity android:name=".GsonTestActivity" />
        <activity android:name=".ArrangePrivateLessonActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestOkHttpActivity" />
        <activity
            android:name=".activity.aaaActivity"
            android:label="@string/title_activity_aaa"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>